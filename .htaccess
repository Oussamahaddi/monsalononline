# Go directly to the Back-end folder.
<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteRule ^$ Backend/libraries/Core.php [L]
    RewriteRule (.*) Backend/libraries/Core.php?url=$1 [L]
</IfModule>